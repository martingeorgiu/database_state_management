name: database_state_management
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  drift: ^2.1.0
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  path: ^1.8.0
  path_provider: ^2.0.2
  sqlite3_flutter_libs: ^0.5.0

dev_dependencies:
  build_runner: ^2.1.1
  drift_dev: ^2.1.0
  flutter_test:
    sdk: flutter
  lint: ^1.6.0

flutter:
  uses-material-design: true
